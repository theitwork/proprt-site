import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Local-only marketing site. Static-exportable if we ever deploy:
  // set `output: "export"` and swap <Image> usage accordingly.
};

export default nextConfig;
